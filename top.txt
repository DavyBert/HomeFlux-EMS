<!DOCTYPE html>

<html lang="nl">
<head>
<meta charset="utf-8"/>
<meta content="width=device-width, initial-scale=1" name="viewport"/>
<script data-origin="settings" src="/homey.js" type="text/javascript"></script>
<style>
    :root {
      color-scheme: dark;
      --hf-bg: #071018;
      --hf-surface: #0e1823;
      --hf-surface-2: #121e2c;
      --hf-surface-3: #182738;
      --hf-border: rgba(255,255,255,.10);
      --hf-border-strong: rgba(79,138,16,.42);
      --hf-text: #f4f7fb;
      --hf-muted: rgba(231,238,246,.72);
      --hf-soft: rgba(231,238,246,.52);
      --hf-accent: #4F8A10;
      --hf-accent-2: #7fc13b;
      --hf-accent-soft: rgba(79,138,16,.16);
      --hf-accent-line: rgba(79,138,16,.35);
      --hf-warn: #f0a04c;
      --hf-warn-soft: rgba(240,160,76,.13);
      --hf-shadow: 0 18px 34px rgba(0,0,0,.24);
      --hf-radius: 18px;
      --hf-radius-sm: 14px;
      --hf-gap: 12px;
    }

    * { box-sizing: border-box; }
    html, body { min-height: 100%; }
    body {
      margin: 0;
      padding: 16px 16px 100px;
      background:
        radial-gradient(circle at top right, rgba(79,138,16,.10), transparent 34%),
        linear-gradient(180deg, #08111a 0%, #071018 100%);
      color: var(--hf-text);
      font-family: Inter, system-ui, -apple-system, BlinkMacSystemFont, "Segoe UI", sans-serif;
    }

    .homey-header {
      margin: 0 0 14px;
      padding: 18px 18px 14px;
      border-radius: 22px;
      border: 1px solid var(--hf-border);
      background: linear-gradient(180deg, rgba(17,28,41,.96), rgba(11,19,29,.94));
      box-shadow: var(--hf-shadow);
    }
    .homey-title {
      margin: 0;
      font-size: 34px;
      line-height: 1.05;
      letter-spacing: -.03em;
    }
    .homey-subtitle {
      margin: 8px 0 0;
      color: var(--hf-muted);
      font-size: 15px;
      line-height: 1.45;
    }

    .tabs {
      position: sticky;
      top: 0;
      z-index: 12;
      display: flex;
      gap: 8px;
      flex-wrap: wrap;
      margin: 0 0 16px;
      padding: 10px;
      border-radius: 20px;
      border: 1px solid var(--hf-border);
      background: rgba(10,17,26,.94);
      backdrop-filter: blur(12px);
      box-shadow: 0 10px 24px rgba(0,0,0,.18);
    }
    .tab {
      min-height: 40px;
      padding: 10px 14px;
      border: 1px solid rgba(255,255,255,.08);
      border-radius: 999px;
      background: rgba(255,255,255,.03);
      color: var(--hf-muted);
      cursor: pointer;
      font: inherit;
      font-weight: 600;
      transition: background .18s ease, color .18s ease, border-color .18s ease, transform .18s ease;
    }
    .tab:hover {
      border-color: rgba(255,255,255,.16);
      color: var(--hf-text);
      transform: translateY(-1px);
    }
    .tab.active {
      background: linear-gradient(180deg, rgba(79,138,16,.23), rgba(79,138,16,.14));
      border-color: var(--hf-border-strong);
      color: #fff;
      box-shadow: inset 0 0 0 1px rgba(255,255,255,.04);
    }

    .panel { display: none; }
    .panel.active { display: block; }

    .grid, .status-grid, .battery-grid {
      display: grid;
      grid-template-columns: repeat(2, minmax(0, 1fr));
      gap: 10px 12px;
    }
    .row { display: grid; grid-template-columns: repeat(2, minmax(0, 1fr)); gap: 10px 12px; }
    .row3 { display: grid; grid-template-columns: 1.4fr 1fr 1fr; gap: 10px 12px; align-items: end; }
    .rate-row { display: grid; grid-template-columns: 1.2fr .75fr .75fr 1fr 1fr 1fr auto; gap: 8px; align-items: end; margin-bottom: 10px; }
    .rate-policy-block { grid-column: 1 / -2; display: grid; grid-template-columns: repeat(3,minmax(0,1fr)); gap: 8px 12px; padding: 10px 12px; border: 1px solid var(--hf-border); border-radius: 12px; background: rgba(79,138,16,.07); }
    .rate-policy-title { grid-column: 1 / -1; font-weight: 700; color: var(--hf-text); font-size: 12px; letter-spacing: .02em; }
    .rate-policy-note { grid-column: 1 / -1; margin: 0; font-size: 11px; color: var(--hf-muted); }
    .rate-conflict-disabled { opacity: .45; }
    .tariff-main { display: grid; grid-template-columns: 1.2fr .75fr .75fr .9fr .9fr auto; gap: 8px; align-items: end; }
    .logic-row { display: grid; grid-template-columns: minmax(130px,.7fr) minmax(0,1.8fr); gap: 10px; padding: 8px 0; border-bottom: 1px solid rgba(255,255,255,.08); }
    .logic-row:last-child { border-bottom: 0; }

    .homey-form-fieldset,
    .card,
    .tariff-card,
    .decision-box {
      margin: 0 0 14px;
      padding: 16px;
      border-radius: var(--hf-radius);
      border: 1px solid var(--hf-border);
      background: linear-gradient(180deg, rgba(15,25,36,.98), rgba(10,18,28,.96));
      box-shadow: var(--hf-shadow);
      min-width: 0;
    }

    .homey-form-fieldset {
      display: block;
    }
    .homey-form-fieldset > .grid,
    .homey-form-fieldset > .row,
    .homey-form-fieldset > .row3 { margin-top: 6px; }
    .homey-form-legend, legend {
      width: 100%;
      margin: 0 0 14px;
      padding: 0;
      border: 0;
      color: #fff;
      font-size: 21px;
      font-weight: 800;
      letter-spacing: -.02em;
    }
    h3 {
      margin: 0 0 8px;
      font-size: 20px;
      line-height: 1.2;
      letter-spacing: -.02em;
    }

    .flow-ref {
      margin-top: 5px;
      color: var(--hf-muted);
      font-size: 10.5px;
      line-height: 1.3;
      opacity: .82;
      overflow-wrap: anywhere;
    }
    .flow-ref code { font-size: 10px; }

    .muted, .ok, .tariff-label, .homey-subtle {
      color: var(--hf-muted);
      font-size: 13px;
      line-height: 1.45;
      overflow-wrap: anywhere;
      word-break: break-word;
    }
    .metric {
      font-size: 28px;
      line-height: 1.1;
      font-weight: 800;
      letter-spacing: -.03em;
      overflow-wrap: anywhere;
    }
    .statusline {
      font-size: 15px;
      line-height: 1.35;
      font-weight: 700;
      margin: 4px 0;
      overflow-wrap: anywhere;
    }
    .logic-key { font-weight: 700; color: #fff; }

    .homey-form-group {
      margin: 0;
      padding: 12px;
      border-radius: var(--hf-radius-sm);
      border: 1px solid rgba(255,255,255,.08);
      background: linear-gradient(180deg, rgba(255,255,255,.035), rgba(255,255,255,.02));
      min-width: 0;
    }
    .status-grid .card,
    .battery-grid .card { margin: 0; padding: 12px; }

    .homey-form-label,
    .tariff-label,
    .homey-form-checkbox span {
      display: block;
      margin-bottom: 7px;
      color: #fff;
      font-size: 13px;
      font-weight: 700;
      line-height: 1.35;
    }

    .homey-form-checkbox {
      display: flex;
      align-items: flex-start;
      gap: 10px;
      margin: 0;
      cursor: pointer;
      color: var(--hf-text);
    }
    .homey-form-checkbox input {
      margin-top: 2px;
      accent-color: var(--hf-accent);
      flex: 0 0 auto;
    }

    .homey-form-input,
    .homey-form-select,
    textarea,
    input[type="time"],
    input[type="number"],
    input[type="text"],
    select {
      width: 100%;
      min-height: 42px;
      border-radius: 12px;
      border: 1px solid rgba(255,255,255,.12);
      background: rgba(6,11,17,.92);
      color: #fff;
      padding: 10px 12px;
      font: inherit;
      outline: none;
      transition: border-color .18s ease, box-shadow .18s ease, background .18s ease;
    }
    textarea { min-height: 140px; resize: vertical; }
    .homey-form-input:focus,
    .homey-form-select:focus,
    textarea:focus,
    input:focus,
    select:focus {
      border-color: rgba(127,193,59,.65);
      box-shadow: 0 0 0 3px rgba(79,138,16,.18);
      background: rgba(10,16,24,.98);
    }

    .input-grid, .day-picker, .day-actions, .savebar {
      display: flex;
      flex-wrap: wrap;
      gap: 8px;
